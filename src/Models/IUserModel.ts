export interface IUserModel {
    id: number,
    firstName: string,
    lastName: string,
    maidenName?: string,
    age?: 50,
    gender?: string,
    email?: string,
    phone?: number,
    birthDate?: number,
    image?: string
}

// Виводжу не всі поля)